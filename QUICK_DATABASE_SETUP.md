# 🚀 Быстрое восстановление базы данных ARTECO

## ✅ Да, можем восстановить базу данных полностью!

У вас есть все SQL скрипты, поэтому восстановление возможно. Вот пошаговый план.

---

## 📋 Вариант 1: Облачный Supabase (РЕКОМЕНДУЕТСЯ - 30-60 минут)

### Шаг 1: Создать проект Supabase (5 минут)

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите или зарегистрируйтесь (можно через GitHub)
3. Нажмите **"New Project"**
4. Заполните:
   - **Name**: `arteco` (или любое другое имя)
   - **Database Password**: создайте сложный пароль (обязательно сохраните!)
   - **Region**: выберите ближайший (например, `West EU (London)` для Европы)
   - **Pricing Plan**: `Free` (для начала)
5. Нажмите **"Create new project"**
6. Подождите 2-3 минуты пока проект создается

### Шаг 2: Получить ключи API (2 минуты)

1. В проекте перейдите в **Settings** → **API** (левое меню)
2. Скопируйте следующие значения:

```
Project URL: https://xxxxxxxxxxxxx.supabase.co
anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... (вверху страницы)
service_role key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... (внизу страницы, секция "service_role")
```

**⚠️ ВАЖНО: Сохраните эти ключи! Они понадобятся для настройки сайта.**

### Шаг 3: Выполнить SQL скрипты (20-30 минут)

1. В проекте перейдите в **SQL Editor** (левое меню)
2. Нажмите **"+ New query"**

#### 3.1. Основная схема базы данных

Откройте файл `setup_beget_database.sql` и скопируйте **ВСЁ** его содержимое в SQL Editor, затем нажмите **Run** (или `Cmd + Enter`).

Это создаст:
- ✅ Основные таблицы (categories, products, orders, profiles и др.)
- ✅ Индексы для производительности
- ✅ Триггеры для автоматического обновления
- ✅ RLS политики для безопасности
- ✅ Тестовые данные

#### 3.2. Дополнительные таблицы и функции

Выполните следующие скрипты **по порядку** (каждый в отдельном запросе):

```sql
-- 1. Аутентификация
-- Скопируйте содержимое: setup_auth_local.sql

-- 2. Дополнительные поля для категорий
-- Скопируйте содержимое: setup_categories_position.sql

-- 3. Журнал (статьи)
-- Скопируйте содержимое: setup_journal.sql

-- 4. Заказы (дополнительные поля)
-- Скопируйте содержимое: setup_orders.sql

-- 5. Партнеры
-- Скопируйте содержимое: setup_partners.sql

-- 6. Логи платежей
-- Скопируйте содержимое: setup_payment_logs.sql

-- 7. Модули товаров
-- Скопируйте содержимое: setup_product_modules.sql

-- 8. Дополнительные поля товаров
-- Скопируйте содержимое следующих файлов по порядку:
-- setup_products_color_links.sql
-- setup_products_cost_price.sql
-- setup_products_custom_size.sql
-- setup_products_fast_delivery.sql
-- setup_products_handles.sql
-- setup_products_interior_images.sql
-- setup_products_is_hidden.sql
-- setup_products_original_price.sql
-- setup_products_rich_content.sql

-- 9. Видео в промо-блоках
-- Скопируйте содержимое: setup_promo_blocks_video.sql

-- 10. Квизы
-- Скопируйте содержимое: setup_quiz.sql
-- Скопируйте содержимое: setup_kitchen_matchmaker_quiz.sql

-- 11. Игры
-- Скопируйте содержимое: setup_game.sql

-- 12. Запросы на обратный звонок
-- Скопируйте содержимое: setup_callback_requests.sql

-- 13. OAuth профили
-- Скопируйте содержимое: setup_oauth_profiles.sql
```

#### 3.3. Настройка Storage (для изображений)

Выполните:

```sql
-- 1. Storage
-- Скопируйте содержимое: setup_storage.sql

-- 2. Бакеты Storage
-- Скопируйте содержимое: setup_storage_bucket.sql

-- 3. Категории Storage
-- Скопируйте содержимое: setup_storage_categories.sql
```

**Также через интерфейс Supabase:**
1. Перейдите в **Storage** (левое меню)
2. Создайте бакеты (если их нет):
   - `products` - публичный доступ для чтения
   - `categories` - публичный доступ для чтения
   - `banners` - публичный доступ для чтения
   - `interiors` - публичный доступ для чтения
   - `journal` - публичный доступ для чтения

#### 3.4. Улучшение RLS политик (безопасность)

Выполните (в порядке):

```sql
-- 1. Базовые политики
-- Скопируйте содержимое: fix_rls_simple.sql

-- 2. Расширенные политики
-- Скопируйте содержимое: fix_rls_policies.sql

-- 3. Полная настройка
-- Скопируйте содержимое: fix_rls_complete.sql
```

#### 3.5. Оптимизация производительности

```sql
-- Скопируйте содержимое: setup_database_indexes_performance.sql
```

### Шаг 4: Обновить переменные окружения (5 минут)

#### Локально:

Создайте или обновите файл `.env.local` в корне проекта:

```bash
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Остальные переменные (Telegram, платежи и т.д. - оставьте как есть)
```

#### На Vercel:

1. Откройте ваш проект на [vercel.com](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Обновите/добавьте:
   - `NEXT_PUBLIC_SUPABASE_URL` → новый URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` → новый anon key
   - `SUPABASE_SERVICE_ROLE_KEY` → новый service_role key
4. Нажмите **Save**
5. Перейдите в **Deployments**
6. Нажмите **"..."** у последнего деплоя → **Redeploy**

### Шаг 5: Проверить работу (5 минут)

#### Локально:

```bash
cd "/Users/anastasiashorohova/.cursor/worktrees/ARTECO/rwJgU"
npm run dev
```

Откройте http://localhost:3000 и проверьте:
- ✅ Главная страница загружается
- ✅ Категории отображаются
- ✅ Товары отображаются (если есть)

#### На Vercel:

После пересборки откройте ваш сайт и проверьте работу.

---

## 📋 Вариант 2: Собственный VPS (4-8 часов)

Если вы хотите установить Supabase на свой VPS (например, на Beget), смотрите подробную инструкцию в файле `DATABASE_RECOVERY_PLAN.md`.

**Требования:**
- VPS с Ubuntu 22.04 LTS
- Минимум 2GB RAM, 1 CPU, 20GB SSD
- Знания системного администрирования

---

## ✅ Чек-лист восстановления

### Подготовка:
- [ ] Создать проект Supabase (или настроить VPS)
- [ ] Сохранить API ключи (URL, anon key, service_role key)

### Выполнение SQL:
- [ ] Выполнить `setup_beget_database.sql` (основная схема)
- [ ] Выполнить все `setup_*.sql` файлы (дополнительные функции)
- [ ] Настроить Storage через интерфейс
- [ ] Выполнить `fix_rls_*.sql` файлы (безопасность)
- [ ] Выполнить `setup_database_indexes_performance.sql` (оптимизация)

### Настройка сайта:
- [ ] Обновить `.env.local` локально
- [ ] Обновить Environment Variables на Vercel
- [ ] Пересобрать проект на Vercel

### Тестирование:
- [ ] Проверить работу сайта локально
- [ ] Проверить работу сайта на Vercel
- [ ] Проверить админ-панель (если есть)

---

## 🆘 Если что-то пошло не так

### Ошибка: "relation already exists"
**Решение:** Это нормально, скрипты используют `CREATE TABLE IF NOT EXISTS`. Продолжайте выполнение.

### Ошибка: "policy already exists"
**Решение:** Удалите старые политики вручную или просто игнорируйте ошибку, если скрипт продолжает работать.

### Ошибка: "Connection refused" на сайте
**Решение:**
1. Проверьте правильность URL и ключей в `.env.local`
2. Убедитесь, что проект Supabase активен
3. Проверьте настройки Firewall в Supabase Dashboard

### Ошибка: "RLS policy violation"
**Решение:**
1. Убедитесь, что выполнили все `fix_rls_*.sql` скрипты
2. Проверьте, что пользователь аутентифицирован (для защищенных таблиц)

---

## 📝 Важные замечания

1. **Данные не восстановятся автоматически** - если у вас не было SQL дампа, товары, категории и пользователи нужно будет добавить заново через админ-панель.

2. **Изображения нужно перезагрузить** - если изображения хранились в Storage, их нужно будет загрузить заново.

3. **Пользователи не сохранятся** - если критично сохранить пользователей, нужно было экспортировать их до удаления базы. В новую базу их нужно будет добавить заново или пользователи зарегистрируются снова.

4. **Заказы не сохранятся** - история заказов не восстановится без SQL дампа.

---

## 🎉 Готово!

После выполнения всех шагов ваша база данных будет полностью восстановлена по тому же принципу, что и была!

**Время восстановления:**
- Облачный Supabase: 30-60 минут
- Собственный VPS: 4-8 часов

**Рекомендация:** Начните с облачного Supabase - это быстрее и проще. При необходимости можно мигрировать на свой VPS позже.
