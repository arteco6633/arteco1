# План оптимизации производительности ARTECO

## 🎯 Приоритеты оптимизации

### 🔴 Критичные (быстрый эффект)
1. **Server Components для главной страницы** - перенести загрузку данных на сервер
2. **Кэширование API запросов** - добавить React Query или SWR
3. **ISR для статических страниц** - категории, продукты
4. **Оптимизация изображений** - добавить preload для критичных, улучшить sizes

### 🟡 Важные (средний эффект)
5. **Code splitting** - динамические импорты для тяжелых компонентов
6. **Prefetching ссылок** - предзагрузка при hover
7. **Skeleton screens** - вместо полноэкранных лоадеров
8. **Оптимизация запросов Supabase** - batch requests, select только нужные поля

### 🟢 Улучшения (дополнительный эффект)
9. **Service Worker** - улучшить кэширование стратегию
10. **Compression** - проверка Gzip/Brotli на Vercel
11. **CDN для статики** - если используется
12. **Database индексы** - проверить индексы в Supabase

---

## 📊 Текущие проблемы

### 1. Главная страница (app/page.tsx)
- ❌ Client Component загружает все данные на клиенте
- ❌ 4 последовательных запроса к Supabase без кэширования
- ❌ Все данные загружаются при каждом посещении
- ✅ Исправление: Использовать Server Components + кэширование

### 2. Страница продукта (app/product/[id]/page.tsx)
- ❌ Client Component, данные загружаются на клиенте
- ❌ Нет ISR, данные запрашиваются каждый раз
- ✅ Исправление: Server Component + ISR

### 3. Каталог (app/catalog/page.tsx)
- ❌ Client Component
- ❌ Категории загружаются каждый раз
- ✅ Исправление: Server Component + ISR

### 4. Изображения
- ✅ Используется next/image
- ⚠️ Нет preload для критичных изображений
- ⚠️ Можно улучшить sizes атрибут

---

## 🚀 Рекомендуемая реализация

### Этап 1: Server Components + Кэширование (высокий приоритет)

1. **Преобразовать главную страницу в Server Component**
   - Загружать данные на сервере
   - Использовать `next/cache` для кэширования
   - Переиспользовать компоненты как Client Components только для интерактивности

2. **Добавить React Query или встроенный кэш Next.js**
   - React Query для клиентских запросов
   - Или использовать `unstable_cache` из Next.js

3. **ISR для статических страниц**
   - `revalidate: 3600` для категорий и продуктов
   - `revalidate: 60` для страниц с часто меняющимся контентом

### Этап 2: Оптимизация загрузки (средний приоритет)

4. **Skeleton screens**
   - Заменить полноэкранные лоадеры на skeleton
   - Улучшить perceived performance

5. **Prefetching**
   - Добавить prefetch для ссылок при hover
   - Preload критичных ресурсов

6. **Code splitting**
   - Динамические импорты для модалов, игр, квизов
   - Lazy load компонентов ниже fold

### Этап 3: Дополнительные улучшения (низкий приоритет)

7. **Service Worker стратегия**
   - Cache-first для статики
   - Network-first для API

8. **Оптимизация запросов**
   - Batch requests где возможно
   - Select только нужные поля

---

## 📈 Ожидаемые результаты

- **First Contentful Paint (FCP)**: -40-60%
- **Largest Contentful Paint (LCP)**: -30-50%
- **Time to Interactive (TTI)**: -20-40%
- **Total Blocking Time (TBT)**: -50-70%
- **Cumulative Layout Shift (CLS)**: останется низким

---

## 🔧 Технические детали

### Server Components в Next.js 15
```typescript
// app/page.tsx
export default async function HomePage() {
  const banners = await getBanners() // Server-side
  const products = await getFeaturedProducts() // Server-side
  
  return (
    <>
      <HeroBanners banners={banners} />
      <ProductGrid products={products} />
    </>
  )
}
```

### Кэширование с unstable_cache
```typescript
import { unstable_cache } from 'next/cache'

const getCachedProducts = unstable_cache(
  async () => {
    const { data } = await supabase.from('products').select('*')
    return data
  },
  ['featured-products'],
  { revalidate: 3600 }
)
```

### ISR для страниц
```typescript
export const revalidate = 3600 // Revalidate every hour
```



