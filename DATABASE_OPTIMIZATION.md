# Оптимизация базы данных для слабого сервера Supabase

## Характеристики сервера
- **CPU:** 2 ядра
- **RAM:** 3 ГБ
- **Storage:** 40 ГБ NVMe
- **Частота:** Standard 3+ GHz

## Влияние на производительность

### ❌ Проблемы слабого сервера на медленном интернете:

1. **Недостаточная RAM (3 ГБ)**
   - PostgreSQL требует минимум 1-2 ГБ для работы
   - Остается мало памяти для кэширования запросов
   - Медленная обработка запросов при нехватке памяти
   - **Влияние:** Запросы выполняются медленнее, увеличивая общее время загрузки

2. **Всего 2 ядра CPU**
   - Одновременная обработка нескольких запросов ограничена
   - При параллельных запросах могут возникать задержки
   - **Влияние:** Параллельные запросы к базе могут выполняться последовательно

3. **Комбинация факторов**
   - Медленный интернет пользователя + медленный сервер = **двойная проблема**
   - Если запрос к БД занимает 2 секунды вместо 0.2 секунды, на медленном интернете это становится критичным

---

## Что уже оптимизировано

### ✅ 1. Оптимизация SELECT запросов
- Выбираются только нужные поля (уменьшение размера ответа в 10-20 раз)
- Не загружаются большие JSONB массивы (handles, fillings, specs и т.д.)

### ✅ 2. Параллельная загрузка критических данных
- Все запросы выполняются через `Promise.all()` для максимальной параллельности

### ✅ 3. Отложенная загрузка некритических данных
- Категории и интерьеры загружаются с задержкой

---

## Дополнительные оптимизации для слабого сервера

### 1. 🔥 КРИТИЧНО: Добавление индексов в базу данных

**Проблема:** Без индексов PostgreSQL выполняет полное сканирование таблиц (Full Table Scan), что очень медленно на слабом сервере.

**Решение:** Создать индексы для часто используемых полей:

```sql
-- Индексы для таблицы products
CREATE INDEX IF NOT EXISTS idx_products_is_featured ON products(is_featured) WHERE is_featured = true;
CREATE INDEX IF NOT EXISTS idx_products_is_new ON products(is_new) WHERE is_new = true;
CREATE INDEX IF NOT EXISTS idx_products_is_hidden ON products(is_hidden) WHERE is_hidden = false;
CREATE INDEX IF NOT EXISTS idx_products_category_id ON products(category_id);
CREATE INDEX IF NOT EXISTS idx_products_category_hidden ON products(category_id, is_hidden);

-- Индекс для promo_blocks
CREATE INDEX IF NOT EXISTS idx_promo_blocks_active ON promo_blocks(is_active) WHERE is_active = true;
CREATE INDEX IF NOT EXISTS idx_promo_blocks_position ON promo_blocks(position, sort_order);

-- Индекс для categories
CREATE INDEX IF NOT EXISTS idx_categories_active ON categories(is_active) WHERE is_active = true;

-- Индекс для client_interiors
CREATE INDEX IF NOT EXISTS idx_interiors_created_at ON client_interiors(created_at DESC);
```

**Влияние:** Ускорение запросов в **10-100 раз** на слабом сервере!

### 2. Ограничение количества данных

**Уже сделано:**
- `.limit(8)` для featured товаров
- `.limit(NEW_PRODUCTS_LIMIT)` для новых товаров

**Можно улучшить:**
- Для медленного соединения можно уменьшить лимиты:
  - Featured: 6 вместо 8
  - New: 6 вместо 8

### 3. Кэширование на клиенте

**Рекомендация:** Использовать localStorage для кэширования данных:
- Кэшировать список категорий (редко меняется)
- Кэшировать список баннеров на 5-10 минут
- Кэшировать товары на 1-2 минуты

### 4. Оптимизация запросов с условиями

**Проблема:** Запросы с несколькими условиями (`eq`, `order`, `limit`) могут быть медленными без правильных индексов.

**Решение:** Композитные индексы для часто используемых комбинаций:
```sql
CREATE INDEX IF NOT EXISTS idx_products_featured_hidden ON products(is_featured, is_hidden) WHERE is_featured = true AND is_hidden = false;
CREATE INDEX IF NOT EXISTS idx_products_new_hidden ON products(is_new, is_hidden, id DESC) WHERE is_new = true AND is_hidden = false;
```

### 5. Использование Connection Pooling

**Проблема:** Каждое подключение к PostgreSQL потребляет память (~10-20 MB).

**Решение:** 
- Использовать Supabase Connection Pooler (уже должно быть включено)
- Ограничить количество одновременных подключений

---

## Проверка производительности

### Как проверить, является ли сервер узким местом:

1. **Проверка времени выполнения запросов:**
   ```javascript
   const start = Date.now()
   const { data } = await supabase.from('products').select('*').limit(8)
   console.log('Query time:', Date.now() - start, 'ms')
   ```
   - **Хорошо:** < 200ms
   - **Приемлемо:** 200-500ms
   - **Плохо:** > 500ms (проблема с сервером или индексами)

2. **Проверка использования ресурсов:**
   - Откройте панель управления сервером
   - Проверьте использование CPU и RAM
   - Если CPU постоянно > 80% или RAM > 90% - сервер перегружен

3. **Анализ медленных запросов:**
   ```sql
   -- В Supabase SQL Editor
   SELECT query, calls, total_time, mean_time
   FROM pg_stat_statements
   ORDER BY mean_time DESC
   LIMIT 10;
   ```

---

## Рекомендации по улучшению сервера

### Краткосрочные (без изменения сервера):

1. ✅ **Добавить индексы** (критично!)
2. ✅ **Оптимизировать запросы** (уже сделано)
3. ⚠️ **Уменьшить лимиты** на медленном интернете
4. ⚠️ **Добавить кэширование** на клиенте

### Долгосрочные (изменение сервера):

1. **Увеличить RAM до 4-8 ГБ**
   - Улучшит кэширование запросов
   - Уменьшит использование swap-памяти
   - Ускорит обработку запросов на 30-50%

2. **Увеличить CPU до 4 ядер**
   - Позволит обрабатывать больше параллельных запросов
   - Ускорит обработку запросов на 50-100%

---

## Создание индексов

Выполните этот SQL скрипт в Supabase SQL Editor:

```sql
-- ===============================================
-- ОПТИМИЗАЦИЯ ИНДЕКСОВ ДЛЯ ПРОИЗВОДИТЕЛЬНОСТИ
-- ===============================================

-- Индексы для таблицы products (критично для главной страницы)
CREATE INDEX IF NOT EXISTS idx_products_is_featured ON products(is_featured) WHERE is_featured = true;
CREATE INDEX IF NOT EXISTS idx_products_is_new ON products(is_new) WHERE is_new = true;
CREATE INDEX IF NOT EXISTS idx_products_is_hidden ON products(is_hidden) WHERE is_hidden = false;

-- Композитные индексы для часто используемых запросов
CREATE INDEX IF NOT EXISTS idx_products_featured_hidden ON products(is_featured, is_hidden, id DESC) 
  WHERE is_featured = true AND is_hidden = false;
CREATE INDEX IF NOT EXISTS idx_products_new_hidden ON products(is_new, is_hidden, id DESC) 
  WHERE is_new = true AND is_hidden = false;

-- Индекс для категорий
CREATE INDEX IF NOT EXISTS idx_products_category_id ON products(category_id);
CREATE INDEX IF NOT EXISTS idx_products_category_hidden ON products(category_id, is_hidden) 
  WHERE is_hidden = false;

-- Индексы для promo_blocks
CREATE INDEX IF NOT EXISTS idx_promo_blocks_active ON promo_blocks(is_active) WHERE is_active = true;
CREATE INDEX IF NOT EXISTS idx_promo_blocks_position_sort ON promo_blocks(position, sort_order) 
  WHERE is_active = true;

-- Индекс для categories
CREATE INDEX IF NOT EXISTS idx_categories_active ON categories(is_active) WHERE is_active = true;

-- Индекс для client_interiors
CREATE INDEX IF NOT EXISTS idx_interiors_created_at ON client_interiors(created_at DESC);

-- Анализ таблиц для обновления статистики
ANALYZE products;
ANALYZE promo_blocks;
ANALYZE categories;
ANALYZE client_interiors;
```

**Важно:** После создания индексов выполните `ANALYZE` для обновления статистики PostgreSQL.

---

## Ожидаемые улучшения после добавления индексов

### До оптимизации (на слабом сервере):
- Время запроса к products: **500-2000ms**
- Время запроса к promo_blocks: **200-800ms**
- Общее время загрузки данных: **2-5 секунд**

### После оптимизации (с индексами):
- Время запроса к products: **50-200ms** (в 5-10 раз быстрее)
- Время запроса к promo_blocks: **20-100ms** (в 5-10 раз быстрее)
- Общее время загрузки данных: **0.3-0.8 секунд** (в 5-10 раз быстрее)

**Итого:** На медленном интернете это даст ускорение в **5-15 раз**!

---

## Мониторинг производительности

### Регулярно проверяйте:
1. Время выполнения запросов (через логи)
2. Использование CPU и RAM сервера
3. Количество активных подключений к БД
4. Размер базы данных (должен быть < 30 ГБ для комфортной работы)

### Признаки проблем:
- ⚠️ CPU постоянно > 80%
- ⚠️ RAM постоянно > 90%
- ⚠️ Запросы выполняются > 500ms
- ⚠️ База данных > 35 ГБ

