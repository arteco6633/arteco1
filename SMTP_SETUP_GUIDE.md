# 📧 Пошаговая инструкция по настройке SMTP

## Вариант 1: Mail.ru (рекомендуется)

### Шаг 1: Включите двухфакторную аутентификацию

1. Перейдите на https://id.mail.ru/profile/security
2. Найдите раздел **"Двухфакторная аутентификация"**
3. Если она не включена, включите её (потребуется подтверждение через телефон)

### Шаг 2: Создайте пароль приложения

1. Перейдите на https://id.mail.ru/profile/security → **"Пароли приложений"**
2. Нажмите **"Создать пароль"**
3. Выберите:
   - **Название**: `ARTECO Email Notifications`
   - **Приложение**: Почта
4. Нажмите **"Создать"**
5. **Скопируйте** сгенерированный пароль (он будет показан один раз!)
   - Пример: `AbCdEfGhIjKlMnOpQrStUvWxYz123456`

### Шаг 3: Добавьте переменные в .env.local

Создайте или откройте файл `.env.local` в корне проекта и добавьте:

```env
# SMTP Configuration для Mail.ru
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=your-email@mail.ru
SMTP_PASSWORD=ваш_пароль_приложения
SMTP_FROM=noreply@arteco.ru
```

**Важно:**
- `SMTP_PASSWORD` - это пароль приложения, НЕ ваш обычный пароль Mail.ru
- `SMTP_USER` - ваш полный email адрес Mail.ru (например, `your-email@mail.ru`, `your-email@inbox.ru`, `your-email@bk.ru`)
- `SMTP_FROM` - можно использовать ваш email Mail.ru или `noreply@arteco.ru` (если домен настроен)

---

## Вариант 2: Gmail

### Шаг 1: Включите двухфакторную аутентификацию

1. Перейдите на https://myaccount.google.com/security
2. Найдите раздел **"Двухэтапная аутентификация"**
3. Если она не включена, включите её (потребуется подтверждение через телефон)

### Шаг 2: Создайте пароль приложения

1. Перейдите на https://myaccount.google.com/apppasswords
   - Если ссылка не работает, попробуйте: https://myaccount.google.com/security → "Пароли приложений"
2. Выберите:
   - **Приложение**: Почта
   - **Устройство**: Другое (введите название: `ARTECO Email Notifications`)
3. Нажмите **"Создать"**
4. **Скопируйте** сгенерированный 16-значный пароль (он будет показан один раз!)
   - Пример: `abcd efgh ijkl mnop`
   - Уберите пробелы: `abcdefghijklmnop`

### Шаг 3: Добавьте переменные в .env.local

```env
# SMTP Configuration для Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ibgroup.salavat@gmail.com
SMTP_PASSWORD=abcdefghijklmnop
SMTP_FROM=noreply@arteco.ru
```

**Важно:**
- `SMTP_PASSWORD` - это пароль приложения (16 символов без пробелов), НЕ ваш обычный пароль Gmail
- `SMTP_FROM` - можно оставить как `ibgroup.salavat@gmail.com` или использовать домен, если он настроен

## Шаг 4: Перезапустите сервер разработки

```bash
# Остановите сервер (Ctrl+C) и запустите снова
npm run dev
```

## Проверка работы

После настройки:
1. Оформите тестовый заказ с указанием email
2. Проверьте почту - должно прийти письмо с благодарностью
3. Измените статус заказа в CRM
4. Проверьте почту - должно прийти уведомление об изменении статуса

## Другие варианты

### Yandex:
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@arteco.ru
```

**Как получить пароль приложения для Yandex:**
1. Перейдите на https://id.yandex.ru/security
2. Найдите раздел **"Пароли приложений"**
3. Создайте новый пароль для "Почта"

### Beget (если есть почтовый ящик на домене):
```env
SMTP_HOST=smtp.beget.com
SMTP_PORT=587
SMTP_USER=noreply@arteco.ru
SMTP_PASSWORD=your-mailbox-password
SMTP_FROM=noreply@arteco.ru
```

**Примечание:** Для Beget используйте обычный пароль от почтового ящика (не нужен пароль приложения)

## Устранение проблем

**Ошибка "Invalid login":**
- Убедитесь, что используете пароль приложения, а не обычный пароль
- Проверьте, что двухфакторная аутентификация включена

**Ошибка "Connection timeout":**
- Проверьте, что порт 587 не заблокирован файрволом
- Попробуйте использовать порт 465 с `secure: true`

**Письма не приходят:**
- Проверьте папку "Спам"
- Убедитесь, что email клиента указан правильно
- Проверьте логи сервера на наличие ошибок

