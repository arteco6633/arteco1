# 🔧 Исправление ошибок 503 и Network Error

## ❌ Почему возникают ошибки?

### Основная причина:
Сайт **всё ещё пытается подключиться к старому Supabase** (`zijajicude.beget.app`), который был удален.

### Что происходит:
1. **503 ошибки (Service Unavailable)** - старый сервер недоступен
2. **Network Error** - нет подключения к базе данных
3. **Ошибки сохранения** - админ-панель не может сохранить данные
4. **CSP нарушения** - не критично, но указывает на проблемы с подключением

### Почему код использует старый URL?
В коде есть **fallback значения** (резервные значения):
- Если переменная окружения `NEXT_PUBLIC_SUPABASE_URL` не установлена → используется `https://zijajicude.beget.app`
- Это происходит, когда переменные окружения не обновлены на Vercel

---

## ✅ Решение: Обновить переменные окружения

### Шаг 1: Убедиться, что новый Supabase создан

Если вы еще не создали новый проект Supabase:
1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Получите API ключи (URL, anon key, service_role key)

Если проект уже создан, переходите к Шагу 2.

---

### Шаг 2: Обновить переменные окружения на Vercel

**Это КРИТИЧЕСКИ ВАЖНО!** Без этого сайт будет продолжать использовать старый Supabase.

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в ваш проект **ARTECO**
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите и обновите **каждую** переменную:

#### 2.1. NEXT_PUBLIC_SUPABASE_URL
- Нажмите на переменную (или создайте, если её нет)
- Замените значение на: `https://xxxxxxxxxxxxx.supabase.co` (ваш новый URL)
- Убедитесь, что выбраны все окружения: `Production`, `Preview`, `Development`
- Нажмите **Save**

#### 2.2. NEXT_PUBLIC_SUPABASE_ANON_KEY
- Нажмите на переменную (или создайте, если её нет)
- Замените значение на ваш новый anon key из Supabase
- Убедитесь, что выбраны все окружения
- Нажмите **Save**

#### 2.3. SUPABASE_SERVICE_ROLE_KEY
- Нажмите на переменную (или создайте, если её нет)
- Замените значение на ваш новый service_role key из Supabase
- Убедитесь, что выбраны все окружения
- Нажмите **Save**

---

### Шаг 3: Пересобрать проект на Vercel

**ОБЯЗАТЕЛЬНО!** После обновления переменных нужно пересобрать проект.

1. В Vercel перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) справа
4. Выберите **"Redeploy"**
5. Подтвердите пересборку
6. Дождитесь завершения деплоя (1-3 минуты)

**Важно:** Без пересборки изменения переменных окружения не применятся!

---

### Шаг 4: Проверить работу

1. Откройте ваш сайт (например, `https://arteeeco.ru`)
2. Откройте консоль браузера (F12 → Console)
3. Проверьте логи:
   - Должен быть новый URL: `Supabase URL: https://xxxxxxxxxxxxx.supabase.co`
   - НЕ должно быть 503 ошибок
   - НЕ должно быть ошибок "Network error"

4. Попробуйте сохранить баннер в админ-панели:
   - Перейдите в `/admin/banners`
   - Попробуйте добавить баннер
   - Должно работать без ошибок

---

## 🔍 Дополнительные ошибки (не критично)

### 1. GoTrueClient warnings
```
Multiple GoTrueClient instances detected
```
**Решение:** Это предупреждение, не ошибка. Можно проигнорировать или исправить позже, объединив экземпляры Supabase клиента.

### 2. CSP нарушения (Content Security Policy)
```
Connecting to 'https://securepay.tinkoff.ru/...' violates CSP
```
**Решение:** 
- Если платежи работают - можно проигнорировать
- Если не работают - нужно обновить `next.config.js`, добавив домен в `connect-src`

### 3. Resource preload warnings
```
The resource was preloaded but not used
```
**Решение:** Это предупреждение о производительности, не критично. Можно проигнорировать.

---

## ✅ Чек-лист исправления

- [ ] Новый проект Supabase создан
- [ ] Получены новые API ключи (URL, anon key, service_role key)
- [ ] Обновлена переменная `NEXT_PUBLIC_SUPABASE_URL` на Vercel
- [ ] Обновлена переменная `NEXT_PUBLIC_SUPABASE_ANON_KEY` на Vercel
- [ ] Обновлена переменная `SUPABASE_SERVICE_ROLE_KEY` на Vercel
- [ ] Проект пересобран на Vercel (Redeploy)
- [ ] Проверена консоль браузера - новый URL используется
- [ ] Нет 503 ошибок
- [ ] Админ-панель работает (можно сохранить баннер)

---

## 🆘 Если ошибки остались

### Проблема: После пересборки всё еще 503 ошибки
**Решение:**
1. Убедитесь, что новый проект Supabase активен
2. Проверьте, что URL правильный (без опечаток)
3. Проверьте, что ключи правильные
4. Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: Переменные не применяются
**Решение:**
1. Убедитесь, что вы нажали **Save** после изменения каждой переменной
2. Убедитесь, что выбраны все окружения (Production, Preview, Development)
3. **Обязательно** сделайте Redeploy после обновления переменных

### Проблема: База данных пустая
**Решение:**
- Это нормально! База данных была удалена вместе со старым Supabase
- Нужно восстановить структуру базы данных (выполнить SQL скрипты)
- См. файл `QUICK_DATABASE_SETUP.md` для инструкции

---

## 📝 Важные замечания

1. **Переменные окружения обновляются только после пересборки проекта**
2. **Локальные изменения (`.env.local`) не влияют на продакшен (Vercel)**
3. **После восстановления базы данных нужно будет добавить данные заново** (товары, категории и т.д.)

---

## 🎉 После исправления

После выполнения всех шагов:
- ✅ Сайт будет подключаться к новому Supabase
- ✅ 503 ошибки исчезнут
- ✅ Админ-панель будет работать
- ✅ Можно будет сохранять данные

**Следующий шаг:** Восстановить структуру базы данных (см. `QUICK_DATABASE_SETUP.md`)
