# Настройка производительности на Vercel

## 🚀 Быстрая настройка

### 1. Включите Analytics

1. Перейдите в настройки проекта на Vercel
2. Откройте вкладку "Analytics"
3. Включите "Web Analytics"
4. Это автоматически включит мониторинг Core Web Vitals

### 2. Проверьте Edge Network

- Edge Network включен по умолчанию на Vercel
- Проверьте в настройках проекта → "Network"

### 3. Настройте Environment Variables

Убедитесь, что все переменные окружения настроены:

```bash
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
```

### 4. Включите Image Optimization (если доступно)

- На Vercel Image Optimization работает автоматически
- Если вы получаете ошибки 402, используйте `unoptimized={true}` для внешних изображений (уже настроено)

## 📊 Мониторинг производительности

### После деплоя проверьте:

1. **Lighthouse Score**:
   - Откройте сайт в Chrome DevTools
   - Запустите Lighthouse
   - Целевые показатели:
     - Performance: 85+
     - Accessibility: 90+
     - Best Practices: 90+
     - SEO: 90+

2. **Core Web Vitals** (в Vercel Analytics):
   - LCP (Largest Contentful Paint): < 2.5s
   - FID (First Input Delay): < 100ms
   - CLS (Cumulative Layout Shift): < 0.1

3. **Bundle Size**:
   - Проверьте размер bundle в Vercel Build Logs
   - Целевой размер: < 500KB для первого загрузки

## 🔧 Дополнительные оптимизации

### Использование ISR (Incremental Static Regeneration)

Для страниц каталога можно добавить ISR:

```typescript
export const revalidate = 3600 // ревалидация каждые 60 минут
```

### Edge Runtime для API Routes

Для быстрых API роутов используйте Edge Runtime:

```typescript
export const runtime = 'edge'
```

## 📈 Ожидаемые результаты

После всех оптимизаций:

- **First Contentful Paint**: улучшение на 30-40%
- **Time to Interactive**: улучшение на 40-50%
- **Bundle Size**: уменьшение на 20-30%
- **Lighthouse Score**: 60-70 → 85-95

## 🐛 Устранение проблем

### Изображения не загружаются

- Проверьте настройки `remotePatterns` в `next.config.js`
- Убедитесь, что домен добавлен в разрешенные

### Service Worker не работает

- Проверьте HTTPS (SW работает только на HTTPS)
- Откройте DevTools → Application → Service Workers
- Проверьте консоль на ошибки

### Медленная загрузка на медленном интернете

- Используйте lazy loading (уже добавлено)
- Проверьте размер изображений
- Убедитесь, что используется кэширование

