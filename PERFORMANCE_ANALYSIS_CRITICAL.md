# КРИТИЧЕСКИЙ АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ БЛОКИРУЮЩИЕ ЗАГРУЗКУ:

### 1. **NAVBAR БЛОКИРУЕТ ВСЕ СТРАНИЦЫ** ⚠️ КРИТИЧНО
- **Проблема**: `Navbar` загружает категории синхронно при монтировании на КАЖДОЙ странице
- **Влияние**: Блокирует рендеринг навигации и всего контента
- **Решение**: Отложенная загрузка категорий (только при открытии меню)

### 2. **ВСЕ СТРАНИЦЫ БЛОКИРУЮТСЯ ДО ЗАГРУЗКИ ДАННЫХ** ⚠️ КРИТИЧНО
- **Проблема**: `if (loading) return <Loader />` на всех страницах
- **Страницы**: 
  - `/catalog/[slug]` - блокируется
  - `/product/[id]` - блокируется  
  - `/journal/[slug]` - блокируется
  - `/catalog` - блокируется
- **Решение**: Показывать страницу сразу, данные подгружать асинхронно

### 3. **ОГРОМНЫЕ ДАННЫЕ ЗАГРУЖАЮТСЯ СРАЗУ** ⚠️ КРИТИЧНО
- **Проблема**: 
  - Интерьеры загружают `gallery_images`, `gallery_previews`, `video_urls` - это МАССИВЫ URL
  - Товары загружают `images`, `colors` - JSONB массивы
  - На медленном интернете это может быть 1-5MB данных
- **Решение**: 
  - Не загружать `gallery_images` и `video_urls` в списке интерьеров
  - Загружать только `cover_image` для списка
  - Полные данные загружать только при открытии детальной страницы

### 4. **НЕТ ТАЙМАУТОВ НА ЗАПРОСЫ** ⚠️ КРИТИЧНО
- **Проблема**: Если Supabase медленный, запрос может висеть минутами
- **Решение**: Добавить timeout на все запросы (10-15 секунд максимум)

### 5. **ЗАГРУЖАЮТСЯ НЕНУЖНЫЕ ДАННЫЕ** ⚠️
- **Проблема**: 
  - В списке товаров загружается `description` (может быть длинным)
  - Загружаются `colors`, `images` массивом для всех товаров
  - Интерьеры загружают все `gallery_images` сразу
- **Решение**: Загружать минимальный набор полей для списков

## 📊 ЧТО УДАЛИТЬ/ОПТИМИЗИРОВАТЬ:

### УДАЛИТЬ:
1. ❌ Синхронную загрузку категорий в Navbar
2. ❌ Блокирующие loading states на всех страницах
3. ❌ Загрузку `gallery_images` и `video_urls` в списке интерьеров
4. ❌ `description` из списков товаров (если не нужен)

### ОПТИМИЗИРОВАТЬ:
1. ✅ Navbar: загружать категории только при открытии меню
2. ✅ Все страницы: показывать сразу, данные подгружать
3. ✅ Интерьеры: загружать только `cover_image` + базовые поля
4. ✅ Товары: убрать `description` из списков, оставить только название и цену
5. ✅ Добавить timeout на все Supabase запросы
6. ✅ Ограничить количество записей на медленном интернете

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЯ:

1. **ВЫСОКИЙ**: Убрать блокирующие loading states
2. **ВЫСОКИЙ**: Оптимизировать Navbar
3. **ВЫСОКИЙ**: Ограничить размер данных интерьеров
4. **СРЕДНИЙ**: Добавить таймауты
5. **СРЕДНИЙ**: Оптимизировать запросы товаров

