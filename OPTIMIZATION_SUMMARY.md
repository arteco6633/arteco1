# Сводка оптимизаций для слабого интернета

## ✅ Что сделано:

### 1. Удалено дублирование:
- ✅ Удален `BrandLoader.tsx` (не используется)
- ✅ Оптимизирован `BootLoader` (удалено дублирование inline loader)
- ⚠️ В `app/page.tsx` есть две функции `loadData` и `loadInteriors` - нужно удалить старые версии

### 2. Оптимизация запросов к БД:
- ✅ Главная страница: используются оптимизированные SELECT с конкретными полями
- ✅ Запросы выполняются параллельно через `Promise.all`
- ✅ Интерьеры загружаются с задержкой на медленном интернете
- ✅ Категории загружаются отложенно
- ✅ Каталог: оптимизирован SELECT для товаров
- ✅ Страница товара: оптимизированы запросы связанных товаров

### 3. Оптимизация загрузки:
- ✅ Тяжелые компоненты (GameModal, KitchenMatchmakerQuiz) загружаются динамически
- ✅ Изображения используют `next/image` с lazy loading
- ✅ Определение скорости интернета для адаптивной загрузки

### 4. Оптимизация изображений:
- ✅ Добавлен `Image` из `next/image` в журнал статей
- ✅ Все изображения используют `unoptimized={true}` для избежания лимитов Vercel

## 🔧 Что нужно доделать:

### КРИТИЧНО - Удалить дублирующиеся функции в `app/page.tsx`:
1. Удалить старую функцию `loadData()` (строки ~143-184) с `SELECT *`
2. Удалить старую функцию `loadInteriors()` (строки ~186-206) с `SELECT *`
3. Обновить `useEffect` для использования оптимизированной логики

### Оптимизировать запросы:
1. `app/product/[id]/page.tsx` - запрос товара (строка 454) можно оставить `SELECT *` для полной загрузки
2. `app/catalog/[slug]/page.tsx` - уже оптимизировано ✅

## 📊 Ожидаемые улучшения:

- **Размер данных:** Уменьшение в 10-20 раз за счет SELECT конкретных полей
- **Время загрузки:** На слабом интернете ускорение в 5-15 раз
- **Первая отрисовка:** Контент показывается быстрее за счет отложенной загрузки некритических данных

