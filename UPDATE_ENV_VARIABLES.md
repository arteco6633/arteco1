# 🔄 Обновление переменных окружения Supabase

## ⚠️ ВАЖНО: Нужно обновить ОБА места!

### 1️⃣ `.env.local` (для локальной разработки)
- Нужен для работы `npm run dev`
- Без него локальная разработка будет использовать старые ключи (fallback значения)

### 2️⃣ Vercel Environment Variables (для продакшена)
- Нужен для работы сайта на Vercel
- Без него продакшен сайт будет использовать старые ключи

---

## 📝 Какие переменные нужно обновить

После создания нового проекта Supabase вам нужно обновить **3 переменные**:

```
NEXT_PUBLIC_SUPABASE_URL=ваш_новый_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_новый_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_новый_service_role_key
```

---

## 🔧 Шаг 1: Обновить `.env.local` (локально)

1. Откройте файл `.env.local` в корне проекта
2. Найдите строки с Supabase:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://zijajicude.beget.app
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

3. Замените их на новые значения из вашего нового проекта Supabase:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_новый_anon_key_из_supabase
SUPABASE_SERVICE_ROLE_KEY=ваш_новый_service_role_key_из_supabase
```

4. Сохраните файл

**Где взять новые ключи:**
- Откройте ваш проект на [supabase.com](https://supabase.com)
- Перейдите в **Settings** → **API**
- Скопируйте:
  - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
  - **anon public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - **service_role key** (внизу страницы) → `SUPABASE_SERVICE_ROLE_KEY`

---

## 🌐 Шаг 2: Обновить Vercel Environment Variables (продакшен)

1. Откройте ваш проект на [vercel.com](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Найдите и обновите каждую переменную:

### 3.1. NEXT_PUBLIC_SUPABASE_URL
- Нажмите на переменную или кнопку **Edit**
- Замените значение на новый URL
- Выберите окружения: `Production`, `Preview`, `Development` (или все)
- Нажмите **Save**

### 3.2. NEXT_PUBLIC_SUPABASE_ANON_KEY
- Нажмите на переменную или кнопку **Edit**
- Замените значение на новый anon key
- Выберите окружения: `Production`, `Preview`, `Development` (или все)
- Нажмите **Save**

### 3.3. SUPABASE_SERVICE_ROLE_KEY
- Если переменная есть - обновите её
- Если переменной нет - нажмите **Add New** и добавьте:
  - **Key**: `SUPABASE_SERVICE_ROLE_KEY`
  - **Value**: ваш новый service_role key
  - **Environments**: `Production`, `Preview`, `Development` (или все)
- Нажмите **Save**

4. После обновления всех переменных:
   - Перейдите в **Deployments**
   - Найдите последний деплой
   - Нажмите **"..."** → **Redeploy**
   - Или подождите автоматической пересборки (если включена)

---

## ✅ Проверка

### Локально:
```bash
cd "/Users/anastasiashorohova/.cursor/worktrees/ARTECO/rwJgU"
npm run dev
```

Откройте http://localhost:3000 и проверьте в консоли браузера (F12):
- Должен быть новый URL в логах: `Supabase URL: https://xxxxxxxxxxxxx.supabase.co`

### На Vercel:
- Откройте ваш сайт
- Проверьте в консоли браузера (F12):
  - Должен быть новый URL в логах
  - Сайт должен подключаться к новой базе данных

---

## ⚠️ Важные замечания

1. **Оба места обязательны:**
   - `.env.local` - для локальной разработки
   - Vercel - для продакшена

2. **Без `.env.local`:**
   - Локальная разработка не будет работать
   - Код будет использовать fallback значения (старые ключи)

3. **Без Vercel переменных:**
   - Продакшен сайт не будет работать
   - Код будет использовать fallback значения (старые ключи)

4. **После обновления на Vercel:**
   - Обязательно пересоберите проект (Redeploy)
   - Иначе изменения не применятся

---

## 🆘 Если что-то не работает

### Проблема: Локально сайт не подключается к базе
**Решение:**
- Проверьте, что `.env.local` обновлен
- Перезапустите `npm run dev`
- Проверьте консоль браузера - какой URL используется

### Проблема: Продакшен сайт не подключается к базе
**Решение:**
- Проверьте, что переменные обновлены на Vercel
- Убедитесь, что проект пересобран (Redeploy)
- Проверьте консоль браузера - какой URL используется

### Проблема: Переменные не применяются на Vercel
**Решение:**
- После обновления переменных обязательно сделайте **Redeploy**
- Или дождитесь следующего автоматического деплоя (если есть изменения в коде)
