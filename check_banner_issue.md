# 🔍 Проверка проблемы с баннерами

## Проблема
Баннер добавлен в админ-панели, но не отображается на главной странице.

## Возможные причины

### 1. Неправильный тип данных в запросе (КРИТИЧНО!)
В файле `app/page.tsx` на строке 212 используется:
```javascript
.eq('is_active', 'true')  // ❌ НЕПРАВИЛЬНО - строка
```

Но `is_active` - это булево поле (BOOLEAN), поэтому нужно:
```javascript
.eq('is_active', true)  // ✅ ПРАВИЛЬНО - булево значение
```

### 2. Данные в базе данных
Проверьте, что баннер действительно сохранен:
- `is_active = true` (не строка 'true')
- `position` установлена (например, 'homepage')
- Все обязательные поля заполнены

### 3. RLS политики
Убедитесь, что RLS политики позволяют читать баннеры публично.

---

## 🔧 Решение

### Шаг 1: Исправить код
Исправить запрос в `app/page.tsx` - заменить строку на булево значение.

### Шаг 2: Проверить данные в базе
Выполнить SQL запрос для проверки баннеров.

### Шаг 3: Проверить RLS политики
Убедиться, что политики настроены правильно.
